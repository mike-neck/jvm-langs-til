buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
         classpath kotlinGradlePlugin
    }
}

apply plugin: '[(${lang})]'

repositories {
    mavenCentral()
    jcenter()
}

dependencies {
    compile     kotlinStdLib
}

apply plugin: 'java'

ext {
    thymeleaf = "$thymeleafGroup:$thymeleafAf:$thymeleafVersion"
}

repositories {
    mavenCentral()
    jcenter()
}

dependencies {
    compile gradleApi()
    compile thymeleaf
    testCompile 'junit:junit:4.12'
}
